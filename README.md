# taskflow-pub

Собранная статика сайта **TaskFlow** — https://task.reneos.com

Файлы в `docs/` генерируются автоматически (GitHub Actions приватного
репозитория с исходниками) и публикуются через GitHub Pages (`main:/docs`).

⚠️ Руками здесь ничего не редактировать — следующий деплой всё перезапишет.
